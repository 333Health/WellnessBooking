333z.one — Standalone Site

This is a standalone, self-contained version of the 333 hero/directory/booking experience — same code that was being pasted into Shopify's Custom HTML section, now packaged as a normal static site. No Shopify theme wrapper, no native popup/modal collisions, no CSS cascade fights. It's one file: index.html, with everything (styles, markup, and JS) inline
